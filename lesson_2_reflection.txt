What happens when you initialize a repository? Why do you need to do it?
it creates the .git directory and start the tracking of the files inside the
folder.

How is the staging area different from the working directory and the repository? What value do you think it offers?
Staging area is not very different from the working directory. the main diffe-
rence is that it contains just the files that you add to stage area.